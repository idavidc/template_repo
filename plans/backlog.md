# Backlog

Raw ideas, one line each. The planner picks items from here and moves them
into `plans/active/` via `new-task.sh`.

Format: `- [ ] <id?> <one-line outcome>`

- [ ] Example: add CI check that fails on lint errors
- [ ] Example: document how to run the test suite locally
